# AGENTS.md - Workspace Rules

- **Git Commit Strict Rule**: NEVER execute `git commit` or `git push`. All Git commits and pushes MUST be performed manually by the USER. The agent should only make code edits and verify builds locally.
